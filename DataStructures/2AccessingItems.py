# this creates a variable called numbers which has a list containing a range of numbers from 0-19 inside it
numbers = list(range(20))
# this prints the list of numbers in reverse
print(numbers[::-1])
